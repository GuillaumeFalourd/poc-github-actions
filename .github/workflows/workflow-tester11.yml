name: Test 11
on:
    pull_request:
        types: [edited, opened, synchronize, reopened]
        branches:
            - dev

defaults:
    run:
        shell: bash

jobs:
    somting:
        runs-on: ubuntu-latest
        steps:
            - run: echo "Test"

name: 34 - Workflow call

on:
  push:

jobs:
  call-workflow-passing-data:
    uses: GuillaumeFalourd/poc-github-actions/.github/workflows/33-reusable-workflow.yml@867fe3013daa4b28fca560acd27dddfe221f93eb
    with:
      username: Guillaume
    secrets:
      token: ${{ secrets.GITHUB_TOKEN }}

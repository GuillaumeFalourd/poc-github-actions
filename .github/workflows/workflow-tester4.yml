name: Test 4

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Run Commands
        run: |
          echo "Hello World"
          echo "This workflow only runs for push to the main branch"